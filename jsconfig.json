// 配置 src 目录的别名为 @
{
    "compilerOptions": {
        "baseUrl": "./",
        "paths": {
            "@/*": [
                "src/*"
            ]
        }
    },
    // 排除以下文件夹
    "exclude": [
        "node_modules",
        "dist"
    ]
}